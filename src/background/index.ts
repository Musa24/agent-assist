/**
 * Service worker entry point (MV3).
 * Routes messages from content scripts and popup, owns the Claude API client,
 * manages the F3 badge, and registers the F4 right-click context menu.
 */
export {};
